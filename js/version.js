/**
 * App version, shown in the footer so deploys are verifiable at a glance.
 * Bump this together with the service worker cache name (sw.js) on every
 * release — the footer updating on your phone means the new deploy (and
 * fresh cache) has actually landed.
 */
export const VERSION = 'v11 · 2026-07-14';
