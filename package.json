{
  "name": "beatbox-to-drums",
  "version": "1.0.0",
  "private": true,
  "description": "Beatbox into your mic and hear it back as drums — entirely in the browser.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test \"test/unit/*.test.mjs\"",
    "smoke": "node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  }
}
